# TODO

Generated by the weekly docs verification workflow. Last updated: 2026-07-15.
Items are carried forward, re-prioritized, or dropped each Monday based on the
current state of the codebase.

## P1 - Correctness and blockers

- [ ] Reconcile `infra/main.parameters.json` with `infra/main.bicep`: the parameters file passes `embeddingDimensions` (`AZURE_OPENAI_EMB_DIMENSIONS`) but `main.bicep` declares no such parameter; either add the param and thread it through to `setup_intvect.py` (which hardcodes 3072) or delete the entry.
- [ ] Fix AGENTS.md drift: frontend entrypoint is `app/frontend/src/index.tsx`, not `src/main.tsx`; `data/` is mostly PDFs, not "Markdown documents"; the backend is aiohttp, but `infra/main.bicep` line 181 comment still says "Python Quart app".
- [ ] `rtmt.py` defines `tools`, `_tools_pending`, and other config as class-level mutable attributes on `RTMiddleTier`; move them into `__init__` to avoid shared state if a second instance is ever created.

## P2 - In-flight and target work

- [ ] Act on the TODO at `app/backend/ragtools.py:80-81`: stop sending full grounding chunks eagerly to the client; send links/ids to the original content in storage instead.
- [ ] Act on the TODO at `app/backend/rtmt.py:131-132`: replace the custom `extension.middle_tier_tool_response` message with a regular text message plus marker so non-aware clients do not break.
- [ ] Wire up or remove the unused `app/frontend/src/components/ui/history-panel.tsx` (and the `HistoryItem` type in `src/types.ts`); nothing imports it.
- [ ] `rtmt.py:190` note: the bearer token provider is synchronous and only warmed at startup; refresh the token on a timer or use an async provider to avoid expiry on long-lived processes.
- [ ] Realtime API version is pinned to `2024-10-01-preview` in `rtmt.py` and `useRealtime.tsx`; evaluate moving to a GA realtime API version and the non-preview model name.
- [ ] The `useDirectAoaiApi` path in `useRealtime.tsx` puts an API key in the WebSocket URL query string; either remove the direct path or document it as insecure-demo-only.

## P3 - Hygiene and hardening

- [ ] Add backend tests (none exist): unit tests for `RTMiddleTier._process_message_to_client`/`_process_message_to_server` message interception and for `ragtools._report_grounding_tool` source sanitization would catch protocol regressions.
- [ ] Add a CI workflow that runs `ruff check app/backend` and `cd app/frontend && npm run build`; currently nothing validates PRs (azure-dev.yml only deploys from main).
- [ ] Replace `print()` calls with the `voicerag` logger in `rtmt.py` (lines 199, 203, 213) and `ragtools.py` (lines 60, 85) to match the logging convention in `app.py`.
- [ ] Pin `gunicorn` and `rich` versions in `app/backend/requirements.txt` (all other deps are pinned); also remove the duplicate `pip install gunicorn` in `app/Dockerfile` (already in requirements).
- [ ] `pyproject.toml` sets ruff `target-version = "py39"` while the app requires Python >= 3.11 (match statements in rtmt.py); bump the target so pyupgrade rules act on the real baseline.
- [ ] Add ruff (and any dev-only tools) to a dev requirements file so `ruff check app/backend` works without an ad hoc install.
- [ ] `azure-search-documents` is pinned to beta `11.6.0b4`; check whether the integrated-vectorization classes used in `setup_intvect.py` are available in a stable release and upgrade.
- [ ] The frontend `es`, `fr`, `ja` locales exist but there is no check that keys stay in sync with `en/translation.json`; add a sync check or script.
- [ ] Set the `CLAUDE_CODE_OAUTH_TOKEN` repository secret so `.github/workflows/update-claude-md.yml` can run (generate with `claude setup-token`).

## Done since last week

- Normalized `app/backend/requirements.txt` from UTF-16 LE to UTF-8, declared the directly-imported `azure-core`, and wired the manifest into the weekly verification workflow (2026-07-15).
- Initial prep-doc set added (CLAUDE.md, PRODUCT.md, ARCHITECTURE.md, root CONTRIBUTING.md, .github/copilot-instructions.md) plus the weekly docs workflow and this TODO seed (2026-07-15).
